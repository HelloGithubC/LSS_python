## Introduction

This project is a simple python package including some useful functions for my work. It is still under development.

## Installation

```bash
pip install ./ --user 
```

Some sub-packages has C/C++ support and need to be compiled. Use `python setup.py build_ext --inplace` to compile them before using pip.